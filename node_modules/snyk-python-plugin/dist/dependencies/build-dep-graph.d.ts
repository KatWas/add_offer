import { DepGraph } from '@snyk/dep-graph';
export interface PartialDepTree {
    name?: string;
    version?: string;
    dependencies?: Dependencies;
    labels?: {
        [key: string]: string;
    };
}
type Dependencies = {
    [depName: string]: PartialDepTree | 'true';
};
export declare function buildDepGraph(partialDepTree: PartialDepTree, projectName?: string): Promise<DepGraph>;
export {};
